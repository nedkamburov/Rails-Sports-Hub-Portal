<section class="home-section main-articles">
  <div class="background-titles">
    <div class="article-category-title"><%= category.title.capitalize %></div>
    <div class="article-news"><%= t('articles.news') %></div>
  </div>
  <section>
    <%= render 'pages/home/preview_article', article: @main_articles.first %>
    <section class="small-card-static-pages d-flex">
      <% if @main_articles.length > 1
           @main_articles.drop(1).each do |article| %>
          <%= render 'pages/home/small_card_breakdown', article: article %>
        <% end %>
      <% end %>
    </section>
  </section>
</section>
<%= render ('pages/home/summary') %>