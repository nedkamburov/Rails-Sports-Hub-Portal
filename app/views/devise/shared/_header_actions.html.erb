<% page_actions = ''
   if devise_mapping.recoverable? && controller_name != 'passwords' && controller_name != 'registrations'
     page_actions = content_tag :div do
       content_tag(:span, 'Don\'t have an account?') +
       content_tag(:a, 'Get Started', href: new_registration_path(resource_name), class: "btn")
     end
   end %>
<section class="header_actions">
  <%= page_actions %>
</section>