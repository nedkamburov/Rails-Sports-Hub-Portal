<%= form_with model: Comment, url: article_comments_path(article) do |form| %>
  <%= form.text_field :content, required: true, placeholder: 'Write a comment', class: "form-control" %>
  <%= form.submit "Submit", class:"btn d-inline-block" %>
<% end %>